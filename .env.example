# Modo de la app: "public" (solo charts, sin backend) o "full" (usa tu backend). Default: full
VITE_MODE=full

# URLs del backend accesible desde el navegador (solo necesarias en modo full)
# Ejemplo local en la misma red (ajusta IP/puertos):
# VITE_API_URL=http://192.168.1.71:8000
# VITE_WS_URL=ws://192.168.1.71:8000
# Ejemplo backend público con TLS:
# VITE_API_URL=https://tu-backend.dominio.com
# VITE_WS_URL=wss://tu-backend.dominio.com
VITE_API_URL=
VITE_WS_URL=
